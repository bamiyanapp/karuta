# Git命名・コミットメッセージのフォーマット規約

新しい作業ブランチを作成する時、またはコミットメッセージを作成する時に使う。
コミットの前提条件（静的チェック・レビュー0件）などのゲート判断自体は `01-development-process.md` を参照し、本ファイルはフォーマットの細部のみを扱う。

## ブランチ命名規則

- 形式: `{type}/{description}`
- type: `feat`, `fix`, `refactor`, `docs`, `chore`, `test`
- description: 簡潔な内容をケバブケースで記述（例: `add-user-auth`）

例: `feat/add-user-auth`, `fix/resolve-memory-leak`

## コミットメッセージのフォーマット

コミットメッセージは必ずヒアドキュメントで渡す（フォーマット崩れを防ぐため）。

### subject

- Conventional Commits 形式を厳守する。
- 必ず1行（50文字以内）で記述する。
- 日本語で記述する。
  例: `feat(auth): 認証機能の追加`

### body

- 必ず空行を1行挟んでから記述する。
- 以下の構造を必ず含める。該当しない場合は `N/A` と記載する。
- 日本語で記述する。

```text
設計:
- 選定内容:
- 却下内容:
- 理由:

影響:
- 影響モジュール:
- 振る舞いの変更:

テスト:
- 追加済み:
- 未追加:
```

レビューで得られた知見は必ず「設計」または「影響」に反映する。
