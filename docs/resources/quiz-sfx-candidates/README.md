# クイズ大会モード 効果音候補（issue #613）

issue #613「回答ボタン・正解・不正解の効果音を管理者・参加者双方で再生する」の実装に使う候補音源。
GitHub上でファイルをタップすると再生プレビューできるため、zipのままではなく個々のmp3として配置している。

まだどのファイルもコードから参照されていない（実装未着手）。実装時に各パックから1ファイルを選び、
`frontend/public/`へコピーして使用する想定（詳細はissue #613参照）。

## 候補と提案マッピング

- 回答ボタン押下 → `Quiz-Button04/`（提案: `Quiz-Button04-1(Fast).mp3`）
- 正解 → `Quiz-Ding_Dong02/`（提案: `Quiz-Ding_Dong02-2(Fast-Single).mp3`）
- 不正解 → `Quiz-Buzzer04/`（提案: `Quiz-Buzzer04-2(Short).mp3`）

マッピング・具体的な採用ファイルは提案であり、実装時に確認する。
